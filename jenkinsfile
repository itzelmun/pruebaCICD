pipeline {

  environment {
    dockerimagename = "itzelmunguia/pruebacicd"
    dockerCredential = 'docker-hub-itzelmun'
    dockerImage = ''
  }

  agent any

  stages {

    stage('Checkout Source') {
      steps {
	git credentialsId: 'github_credential', url: 'https://github.com/itzelmun/pruebaCICD.git'	
      }
    }

    stage('Build image') {
      steps {
        script {
          dockerImage = docker.build dockerimagename
        }
      }
    }

    stage('Pushing Image') {
      environment {
               registryCredential = 'dockerhublogin3'
           }
      steps{
        script {
          docker.withRegistry( 'https://registry.hub.docker.com', registryCredential ) {
            dockerImage.push("latest")
          }
        }
      }
    }

    stage('Deploying App to Kubernetes') {
      steps {
	script {
           kubernetesDeploy(configs: "deployment-service-simplesaml.yaml", kubeconfigId: "kubersanchez")
          }
         }
        }
   stage('Restarting POD') {
      steps {
          sshagent(['kubernetessh']) {
    
        script {
            try{
                sh 'ssh sanchez@148.213.5.79 microk8s.kubectl rollout restart deployment prueba-gha2 --kubeconfig=/home/sanchez/.kube/config'
              }catch(error){
        }
	}
      }
    }
  }
 }
}
